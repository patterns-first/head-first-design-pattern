# 패턴 섞어 쓰기

- 복합 패턴: 여러가지 문제의 일반적인 해결법을 제시하는 것
- 복합패턴의 왕: MVC

# 오리 시뮬레이션 게임에 다양한 패턴 적용하기

- 우선 Quackable 인터페이스 만들기

```java
public interface Quackable {
  public void quack();
}
```

2. Quackable을 구현해서 오리 클래스 만들기, 구상 오리 클래스 만들기

```java
public class MallardDuck implements Quackable {
  public void quack() {
    System.out.printIn("꽥꽥");
  }
}

public class RedheadDuck implements Quackable {
  public void quack() {
    System.out.printIn("꽦꽦");
  }
}
```

- 종류가 다른 Duck 객체도 추가

3. 시뮬레이터 만들기
4. 시뮬레이터에 거위도 추가하기
5. 거위용 어댑터

```java
public class GooseAdapter implements Quackable {
  Goose goose;

  public GooseAdapter(Goose goose) {
    this.goose = goose;
  }

  public void quack() {
    goose.honk();
  }
}
```

6. 시뮬레이터에 거위 추가
7. 잘 동작함
8. 꽥꽥 소리를 낸 횟수 세 주는 기능

- 새로운 행동을 추가하려면 데코레이터를 만들어서 그 데코레이터 객체로 감싸면 Duck 코드는 건드리지 않아도 됨

9. 시뮬레이터를 고쳐서 모든 오리를 데코레이터로 감싸주기

- 모든 duck을 new QuackCounter로 감싸야 함
  => 오리 생성하고 데코레이터로 감싸는 부분을 빼서 캡슐화 하려면?

10. 오리를 생산하는 팩토리 필요

- 여러 종류의 오리를 생산해야 하므로 추상 팩토리 패턴을 사용해야 함

11. 팩토리를 쓰도록 시뮬레이터 고치기

- 추상 팩토리 사용
- 팩토리를 받아서 다형성을 갖추면서 객체를 생성하는 메소드 필요
- 오리 무리를 관리하는 기능?
  - 오리들로 구성된 컬렉션이나 그 컬렉션의 부분 컬렉션을 다룰 수 있는 방법 필요

12. 오리 무리 만들기

- 객체들로 구성된 컬렉션을 개발 객체와 같은 방식으로 다룰 수 있게 하는 컴포지트 패턴

13. 복합 객체 완성

- 오리들이 복합 구조 안으로 들어가도록 코드를 고쳐야 함
- 반대로 오리들을 한마리씩 챙길 수 있도록, 꽥꽥 소리내는 오리들을 1마리씩 실시간으로 추적하는 기능 => 옵저버 패턴

14. Observable 인터페이스 필요
15. Quackable을 구현하는 모든 구상 클래스에서 QuackObservable에 있는 메소드 구현하도록 수정
16. Observer 보조 객체와 Quackable 클래스 결합

# 복합 패턴의 왕 알현하기

- 모델
  - 애플리케이션 존재의 의미
  - 데이터, 논리가 전부 들어있음
  - 어떤 뷰에서도 재사용 가능

- 뷰
  - 표시하고 편집하는 컨트롤
  - 모델을 모름

# 모델-뷰-컨트롤러 알아보기

- 모델: Player
- 뷰: 디스플레이
- 컨트롤러: 모델 조작

# 모델-뷰-컨트롤러 조금 더 알아보기

- 뷰: 모델을 표현하는 방법 제공
- 컨트롤러: 사용자로부터 입력을 받으며 입력 받은 내용이 모델에게 어떤 의미가 있는지 파악
- 모델: 모델에는 모든 데이터, 상태와 어플리케이션 로직
- 사용자는 뷰에만 접촉 가능
- 컨트롤러가 모델에게 상태 변경하라고 요청
- 컨트롤러가 뷰 변경해달라고 요청
- 상태가 변경되면 모댈이 뷰에게 그 사실을 알림
- 뷰가 모델에게 상태를 요청

# 모델-뷰-컨트롤러에서 사용ㅎ되는 패턴 앏아보기

## 전략패턴

- 뷰와 컨트롤러는 고전적인 전략 패턴으로 구현
- 뷰 객체를 여러 전략을 써서 설정 가능
- 컨트롤러가 전략 제공
- 뷰는 애플리케이션의 겉모습에만 신경을 쓰고, 인터페이스의 행동을 결정하는 모든 일은 컨트롤러에게

## 컴포지트 패턴

- 디스플레이는 여러 단계로 겹쳐있는 윈도우, 패널, 버튼, 텍스트 레이블 등으로 구성

## 옵저버 패턴

- 모델은 옵저버 패턴을 써서 상태가 변경되었을 때 그 모델과 연관된 객체들에게 연락

# 모델, 뷰, 컨트롤러 만들기

- BeatModel
- 현재 BPM을 관리하는 상태와 소리를 내는 여러 가지 미디를 생성하는 코드 필요
- 컨트롤러에서 비트를 조절하거나 뷰와 컨트롤러에서의 모델의 상태를 알아낼 때 사용할 수 있도록 외부에 공개된 인터페이스 필요
