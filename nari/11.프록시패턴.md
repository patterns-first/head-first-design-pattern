- 뽑기 기의 재와 현재 상태를 알려주는 기능 추가하기

# 모니터링 코드 만들기

- Gumball machine 클래스에 뽑기 기계의 현재 위치를 알려주는 기능 추가

# 모니터링 기능 테스트

- Gumball Monitor 클래스의 인스턴스를 만들고 모니터링이 필요한 뽑기 기계 넘겨주기
- 그런데 원하는 요구하항은 뽑기 기계를 원격으로 모니터링 하는 것
- 원격 프록시를 활용해 Gumball Monitor 클래스의 프록시를 레퍼런스로 건네주자

# 원격 프록시의 역할

- 원격 프록시는 원격 객체의 로컬 대변자 역할
- 원격 객체: 다른 자바 가상 머신의 힙에서 살고 있는 객체
- 로컬 대변자: 로컬 대변자의 어떤 메소드를 호출하면 다른 원격 객체에게 그 메소드 호출을 전달해주는 객체
  => 클라이언트 객체는 원격 객체의 메소드 호출을 하는 것처럼 행동. 하지만 실제로는 로컬힙에 들어있는 '프록시'객체의 메소드를 호출

# 모니터링에 원격 프록시 추가하기

- 자바 원격 메소드 호출 사용(RMI)

# 원격 메소드의 기초

- 로컬 객체의 메소드를 호출하면 그 요청을 원격 객체에 전달해주는 시스템 디자인?
- 통신을 처리해주는 보조 객체 필요. 보조 객체를 사용하면 클라이언트는 로컬 객체의 메소드만 호출하면 됨
- 클라이언트 보조 객체 -> 원격 객체에게 전달 -> 서버는 보조 객체가 있어서 socket 연결로 클라이언트 보조 객체로부터 요청 처리

# 메소드 호출 과정

1. 클라이언트 객체에서 클라이언트 보조 객체의 doBigThing 호출
2. 클라이언트 보조 객체는 메소드 호출 정보를 잘 포장해서 네트워크로 서비스 보조 객체에게 전달
3. 서비스 보조 객체는 클라이언트 보조 객체로부터 받은 정보를 해석해서 어떤 객체의 어떤 메소드를 호출할지 알아낸 다음 진짜 서비스 객체의 메소드 호출
4. 서비스 객체의 메소드 실행이 끝나면 서비스 보조 객체에 결과 리턴
5. 서비스 보조 객체는 호출 결과로 리턴된 정보를 포장해 네트워크로 클라이언트 보조 객체에게 전달
6. 클라이언트 보조 객체는 받은 정보를 해석해 클라이언트 객체에 전달

# 원격 서비스 만들기

1. 원격 인터페이스 만들기
2. 서비스 구현 클래스 만들기
3. RMI 레지스트리 실행하기
4. 원격 서비스 실행하기

이거 온통 자바잖아..?

# 서버에 필요한 코드 살펴보기

- 원격 인터페이스
- 원격 서비스를 구현한 클래스
  [작동방식]
- 클라이언트에서 RMI 레지스트리 룩업
- RMI 레지스트리에서 스텁 객체 리턴
- 클라이언트는 스텁의 메소드 호출

# 뽑기 기계용 원격 프록시 고민하기

- Gumball Monitor - Gumball stub
- GumballSkeleton -> GumballMachine

# GumballMachine 클래스를 원격 서비스로 바꾸기

- GumballMachine의 원격 인터페이스 만들기
- 인터페이스의 모든 리턴 형식을 직렬화할 수 있는지 확인
- 구상 클래스에서 인터페이스 구현

# RMI 레지스트리 등록하기

# GumballMonitor 클라이언트 코드 고치기

# 새로운 모니터링 기능 테스트

# 프록시 패턴의 정의

- 특정 객체로의 접근을 제어하는 대리인을 제공
- 원격 프록시, 가상 프록시, 보호 프록시 가능

# 원격 프록시와 가상 프록시 비교

- 원격 프록시 : 다른 jvm에 있는 객체의 대리인에 해당하는 로컬 객체
- 가상 프록시 : 생성하는 데 많은 비용이 드는 객체 대신

# 앨범 커버 뷰어 만들기

- 아이콘 만들기 + 아이콘 객체로 하여금 네트워크로 이미지 불러오도록 처리 가능
  => 가상 프록시 사용
- 가상 프록시가 아이콘 대신 백그라운드에서 이미지를 불러오고
- 이미지를 완전히 불러오기 전에는 로딩 메시지 보여주기
- 로딩이 끝나면 프록시가 아이콘 객체에게 작업을 넘김

# 앨범 커버 가상 프록시 설계하기

1. 이미지 프록시는 이미지 아이콘을 생성하고 네트워크 url로부터 이미지를 불러옴
2. 이미지를 가져오는 동안 로딩 메시지
3. 이미지 로딩 끝나면 모든 메소드 호출을 이미지 아이콘 객체에게 넘김
4. 새로운 이미지 요청이 들어오면 프록시를 새로 만들고 반복

# 보호 프록시 만들기

- 자바의 java.lang,reflect 패키지 내 프록시 기능
- 동적 프록시를 활용해 보호 프록시 만들기

# 객체마을 데이팅 서비스

- Person 인터페이스 코드 만들기
  => 보호 프록시를 활용해 접근 권한을 바탕으로 객체로의 접근을 제어
  ex. 회사 직원을 나타내는 객체가 있다면 일반 직원 객체에서 호출할 수 있는 메소드가 정해져 있고, 관리자 객체의 권한은 또 다름

# Person 인터페이스용 동적 프록시 만들기

- 2개의 invocationHandler 만들기
- 동적 프록시 생성 코드 만들기
- 적절한 프록시로 Person 객체 감싸기
